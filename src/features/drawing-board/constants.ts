import type { StrokeConfig } from "./types";

export const DEFAULT_COLORS: StrokeConfig["colors"] = ["#000", "#fff"];
export const DEFAULT_FONT_SIZE = 24;
export const DEFAULT_ZOOM = 1;
export const ZOOM_MIN = 0.5;
export const ZOOM_MAX = 3;
